# Custom packages and overrides
#
# Add packages here that aren't in nixpkgs, or override existing ones.
# Import this in flake.nix when you have overlays to apply.
#
# Example:
#
# final: prev: {
#   my-tool = final.callPackage ./my-tool.nix { };
# }

final: prev: { }
